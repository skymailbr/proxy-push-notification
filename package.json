{
  "name": "push-proxy-notification",
  "version": "1.0.0",
  "description": "Repository for push proxy notification",
  "main": "",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "validate": "swagger-cli validate ./swagger/spec.yaml",
    "build": "redoc-cli bundle -t ./swagger/template.hbs ./swagger/spec.yaml -o index.html --options.suppressWarnings",
    "serve": "redoc-cli serve -t ./swagger/template.hbs ./swagger/spec.yaml -o index.html --options.suppressWarnings --ssr --watch"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/skymailbr/proxy-push-notification.git"
  },
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/skymailbr/proxy-push-notification/issues"
  },
  "homepage": "https://github.com/skymailbr/proxy-push-notification#readme",
  "dependencies": {
    "redoc-cli": "^0.9.9",
    "swagger-cli": "^4.0.4"
  }
}
